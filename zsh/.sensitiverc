
FILE=$(pwd)/.secrets

if [ ! -f $FILE ]; then
    ENC_FILE=$(pwd)/.encrypted
    DEC_FILE=$(pwd)/.secrets

    echo "Decrypting secrets"
    decrypt $ENC_FILE $DEC_FILE
fi

source ~/dotfiles/zsh/.secrets
