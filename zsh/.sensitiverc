
FILE=~/.dotfiles/zsh/.secrets

if [ ! -f $FILE ]; then
    ENC_FILE=~/.dotfiles/zsh/.encrypted
    DEC_FILE=~/.dotfiles/zsh/.secrets

    echo "Decrypting secrets"
    decrypt $ENC_FILE $DEC_FILE
fi

source $FILE
