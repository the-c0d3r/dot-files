# Linux functions

# Keyboard key repeat speed
# xset r rate [delay] [rate]
[ -x "$(command -v xset)" ] && xset r rate 300 15


# If the following is not at the end of the file, then it will show the annoying
# xmodmap:  please release the following keys within 2 seconds:
[ -x "$(command -v xmodmap)" ] && xmodmap -e 'clear Lock'
[ -x "$(command -v xmodmap)" ] && xmodmap -e 'keycode 0x42=Escape'
[ -x "$(command -v xmodmap)" ] && xmodmap -e 'keycode 0x9=Escape'
